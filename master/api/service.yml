apiVersion: v1
kind: Service
metadata:
  labels:
    app: api
  name: api-master
  namespace: master
spec:
  type: ClusterIP
  ports:
    - port: 8000
      protocol: TCP
      targetPort: 8000
  selector:
    app: api
